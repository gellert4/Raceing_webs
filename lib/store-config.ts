/** Keep collection closed until controller details, consent and mail delivery are ready. */
export const launch = {
  waitlistEnabled: false,
  checkoutEnabled: false,
  legalVersion: "2026-09-16-draft-2",
} as const;
